# /next-ticket — Recommend Next Ticket

Runs `lanegate next` to recommend the highest-priority unblocked ticket(s), respecting `depends_on` and the `touches` lock so parallel agents don't collide.

```bash
lanegate next
```
