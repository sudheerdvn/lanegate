#!/usr/bin/env sh
# run-tests.sh — project test runner (scaffolded by lanegate init)
#
# This is a no-op starter.  Replace the body below with the commands that
# run your project's test suite.  The script is executed from the ticket
# worktree directory.
#
# Exit 0  → tests passed (safeguard allows the transition)
# Exit !0 → tests failed (safeguard blocks the transition)

set -e

echo "run-tests.sh: no tests configured — exiting 0 (no-op starter)"
exit 0
